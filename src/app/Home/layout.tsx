import type { Metadata } from "next";
import SideBar from "./_components/SideBar";
import PlayBar from "./_components/PlayBar";
import Navbar from "./_components/Navbar";
import PlayNextSongAfterEnd from "./_components/PlayNextSongAfterEnd";
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};
const Layout = ({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) => {
  return (
    <html lang="en">
    <body>
      <div className="w-screen h-screen flex flex-col ">
        <div className="w-full h-[90vh] flex-auto  flex ">
          <SideBar/>
          <div className="lg:w-[80%] w-[100%] h-full flex flex-col justify-start bg-black text-white ">
           <Navbar/> 
            {children}
            <PlayNextSongAfterEnd />
            </div>
        </div>
        <PlayBar/>
      </div>
     </body>
   </html>
  );
};

export default Layout;
